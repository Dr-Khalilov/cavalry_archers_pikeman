/*
import { ACTION_TYPES } from './types';

export const startGame = (choices) => {
    return {
        type: ACTION_TYPES.START_GAME,
        payload: choices,
    };
};

export const resetGame = () => {
    return {
        type: ACTION_TYPES.RESET_GAME,
    };
};*/
